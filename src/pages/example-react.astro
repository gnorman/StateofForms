---
// Example page showing how to integrate React components
import Layout from '../layouts/Layout.astro';
import StockTicker from '../components/react/StockTicker';
import CryptoChart from '../components/react/CryptoChart';

// This is an example - you can delete this file or use it as reference
---

<Layout title="React Integration Example" description="Example page showing React component integration">
  <section class="section">
    <div class="container">
      <h1>React Component Integration Example</h1>
      <p>This page demonstrates how to use the React components in your Astro pages.</p>

      <div class="card" style="margin-top: 2rem;">
        <h2>How to Use React Components</h2>
        <p>React components need a <code>client:</code> directive to become interactive:</p>
        
        <pre style="background: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>---
import StockTicker from '../components/react/StockTicker';
---

&lt;StockTicker client:load /&gt;</code></pre>

        <h3 style="margin-top: 1.5rem;">Client Directives:</h3>
        <ul style="padding-left: 1.5rem;">
          <li><strong>client:load</strong> - Loads immediately (best for above-the-fold content)</li>
          <li><strong>client:idle</strong> - Loads when browser is idle (good for non-critical widgets)</li>
          <li><strong>client:visible</strong> - Loads when scrolled into view (best for below-the-fold)</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Example 1: Stock Ticker with client:load -->
  <section class="section section-alt">
    <div class="container">
      <h2>Example 1: Stock Ticker (client:load)</h2>
      <p>This loads immediately when the page loads:</p>
      
      <StockTicker client:load />
    </div>
  </section>

  <!-- Example 2: Crypto Chart with client:visible -->
  <section class="section">
    <div class="container">
      <h2>Example 2: Crypto Chart (client:visible)</h2>
      <p>This loads only when you scroll to it (better for performance):</p>
      
      <CryptoChart client:visible symbol="BTC" />
    </div>
  </section>

  <!-- Example 3: Multiple Charts -->
  <section class="section section-alt">
    <div class="container">
      <h2>Example 3: Multiple Charts</h2>
      <p>You can render multiple instances with different props:</p>
      
      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
        <CryptoChart client:visible symbol="BTC" />
        <CryptoChart client:visible symbol="ETH" />
      </div>
    </div>
  </section>

  <!-- Integration Instructions -->
  <section class="section">
    <div class="container">
      <div class="card" style="background: #f9fafb;">
        <h2>Next Steps: Connect to QueryForge API</h2>
        <p>To connect these components to your real QueryForge API:</p>
        
        <ol style="padding-left: 1.5rem;">
          <li style="margin-bottom: 0.75rem;">
            <strong>Open the React component files:</strong>
            <ul style="padding-left: 1.5rem; margin-top: 0.5rem;">
              <li><code>src/components/react/StockTicker.tsx</code></li>
              <li><code>src/components/react/CryptoChart.tsx</code></li>
            </ul>
          </li>
          
          <li style="margin-bottom: 0.75rem;">
            <strong>Uncomment the API fetch functions</strong> in each component
          </li>
          
          <li style="margin-bottom: 0.75rem;">
            <strong>Update the API endpoints</strong> to match your QueryForge API:
            <pre style="background: white; padding: 0.5rem; margin-top: 0.5rem; border-radius: 0.25rem;"><code>const response = await fetch('https://queryforge.ai/api/your-endpoint');</code></pre>
          </li>
          
          <li style="margin-bottom: 0.75rem;">
            <strong>Test with your data</strong> using the development server
          </li>
          
          <li>
            <strong>Deploy</strong> to your DigitalOcean droplet once everything works!
          </li>
        </ol>

        <div style="margin-top: 2rem; padding: 1rem; background: white; border-left: 4px solid #2563eb; border-radius: 0.25rem;">
          <strong>💡 Tip:</strong> Use environment variables for API keys and endpoints. 
          See <code>DEVELOPMENT.md</code> for details.
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container text-center">
      <h2>Ready to Build?</h2>
      <p>Check out the other pages to see the complete site structure.</p>
      <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-top: 1.5rem;">
        <a href="/" class="btn btn-primary">Back to Home</a>
        <a href="/portfolio" class="btn btn-secondary">View Portfolio</a>
      </div>
    </div>
  </section>
</Layout>
